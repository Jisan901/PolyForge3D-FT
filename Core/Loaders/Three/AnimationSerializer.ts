import { THREE } from '@/Core/lib/THREE'


export class AnimationSerializer{
    static serialize(clip: THREE.AnimationClip){
        
    }
    static deserialize(buffer: ArrayBuffer|Uint8Array):THREE.AnimationClip {
        
    }
}